# Feature: Project Setup & Environment Configuration

> **Spec file:** `specs/00-setup.md`
> **Branch:** `main`
> **Day:** 1 — Monday 11/05/2026
> **User Stories covered:** SETUP-1 → SETUP-7

---

## What I want

A clean Laravel 13 project fully configured and ready for development with:
- Laravel 13 installed with PHP 8.4+
- Laravel Breeze installed for authentication scaffolding (register/login/logout only)
- MySQL 8 database connected and configured via `.env`
- Laravel Debugbar installed as a dev dependency
- All 4 migrations created (users, domains, concepts, generated_questions) and running without errors
- `.env.example` updated with `GROQ_API_KEY=` placeholder
- `AGENTS.md` committed as the very first commit
- GitHub repository created with the correct branch structure
- The base `app.blade.php` layout file created with a nav bar and flash message partial

---

## What I do NOT want

- ❌ Do NOT install Vue, React, Livewire, Inertia, or Alpine.js
- ❌ Do NOT install any LLM or AI SDK package — Groq will be called with raw `Http::` only
- ❌ Do NOT modify the `users` table migration generated by Breeze
- ❌ Do NOT create any seeders or factories at this stage
- ❌ Do NOT change Vite config or add any new JS dependencies
- ❌ Do NOT commit `.env` — it must be in `.gitignore`
- ❌ Do NOT use Bootstrap — use plain CSS or Tailwind only
- ❌ Do NOT generate any placeholder/example controllers beyond what Breeze provides

---

## Acceptance Criteria

- [ ] `php artisan --version` returns Laravel 13.x
- [ ] `php artisan migrate` runs with zero errors — all 4 tables created
- [ ] `php artisan serve` starts the app without errors
- [ ] Visiting `/register` and `/login` works (Breeze pages render correctly)
- [ ] `.env` is in `.gitignore` and NOT committed
- [ ] `.env.example` contains `GROQ_API_KEY=` (empty, no value)
- [ ] `AGENTS.md` exists at the repo root and is the first commit
- [ ] Debugbar appears in the browser (dev environment only)
- [ ] GitHub repo has branches: `main`, `feature/auth`, `feature/domains-crud`, `feature/concepts-crud`, `feature/ai-generation`, `feature/dashboard`, `feature/soft-deletes`
- [ ] `resources/views/layouts/app.blade.php` exists and includes the flash messages partial
- [ ] `resources/views/partials/flash-messages.blade.php` exists and displays both `session('success')` and `session('error')`

---

## Data Involved

### Migration: `create_domains_table`
```sql
id              BIGINT UNSIGNED PK AUTO_INCREMENT
user_id         BIGINT UNSIGNED NOT NULL FK → users.id CASCADE DELETE
name            VARCHAR(255) NOT NULL
color           VARCHAR(7) NOT NULL   -- hex color e.g. #3B82F6
created_at, updated_at
```

### Migration: `create_concepts_table`
```sql
id              BIGINT UNSIGNED PK AUTO_INCREMENT
domain_id       BIGINT UNSIGNED NOT NULL FK → domains.id CASCADE DELETE
title           VARCHAR(255) NOT NULL
explanation     TEXT NOT NULL
difficulty      ENUM('junior','mid','senior') NOT NULL DEFAULT 'junior'
status          ENUM('to_review','in_progress','mastered') NOT NULL DEFAULT 'to_review'
deleted_at      TIMESTAMP NULL         -- soft deletes
created_at, updated_at
```

### Migration: `create_generated_questions_table`
```sql
id              BIGINT UNSIGNED PK AUTO_INCREMENT
concept_id      BIGINT UNSIGNED NOT NULL FK → concepts.id CASCADE DELETE
questions       JSON NOT NULL
created_at, updated_at
```

### Migration rules (MANDATORY):
- Use `$table->foreignId('user_id')->constrained()->cascadeOnDelete()`
- Use `$table->foreignId('domain_id')->constrained()->cascadeOnDelete()`
- Use `$table->foreignId('concept_id')->constrained()->cascadeOnDelete()`
- Use `$table->softDeletes()` on `concepts` table ONLY
- NEVER use `$table->foreign()` old syntax

---

## Routes Involved

None at this stage — Breeze auto-registers auth routes.  
Verify they exist with `php artisan route:list | grep auth`.

---

## File Structure Expected After This Step

```
interviewprep/
├── AGENTS.md                          ← first commit
├── .env.example                       ← contains GROQ_API_KEY= (no value)
├── .env                               ← gitignored, contains real DB credentials
├── specs/
│   └── 00-setup.md                    ← this file
├── app/
│   ├── Models/
│   │   ├── User.php
│   │   ├── Domain.php                 ← empty for now, will be filled in 02
│   │   ├── Concept.php                ← empty for now, will be filled in 03
│   │   └── GeneratedQuestion.php      ← empty for now, will be filled in 04
├── database/migrations/
│   ├── xxxx_create_users_table.php    ← Breeze — do NOT touch
│   ├── xxxx_create_domains_table.php
│   ├── xxxx_create_concepts_table.php
│   └── xxxx_create_generated_questions_table.php
└── resources/views/
    ├── layouts/
    │   └── app.blade.php
    └── partials/
        └── flash-messages.blade.php
```

---

## Commands to Run (in order)

```bash
# 1. Create Laravel project
composer create-project laravel/laravel interviewprep

# 2. Install Breeze
composer require laravel/breeze --dev
php artisan breeze:install blade
npm install && npm run build

# 3. Install Debugbar (dev only)
composer require barryvdh/laravel-debugbar --dev

# 4. Create models with migration
php artisan make:model Domain -m
php artisan make:model Concept -m
php artisan make:model GeneratedQuestion -m

# 5. Run migrations
php artisan migrate
```

---

## Flash Messages Partial (exact implementation required)

```blade
{{-- resources/views/partials/flash-messages.blade.php --}}
@if (session('success'))
    <div class="alert alert-success">
        {{ session('success') }}
    </div>
@endif

@if (session('error'))
    <div class="alert alert-error">
        {{ session('error') }}
    </div>
@endif
```

This partial MUST be included in `layouts/app.blade.php` — not in individual views.

---

## Agent Output — What Was Generated
> *(Fill this in after the agent runs)*

---

## What I Changed Manually
> *(Fill this in after the agent runs — what you edited and why)*
